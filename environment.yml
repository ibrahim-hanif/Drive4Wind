name: drive4wind-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytest
  - black
  - flake8
  - pip:
    - packaging  # Only needed for version validation utility
